services:
  - type: web
    name: aml-admin
    env: java
    rootDir: aml-admin
    buildCommand: ./mvnw clean package || mvn clean package
    startCommand: java -jar target/*.jar
    plan: starter
    envVars:
      - key: SPRING_DATASOURCE_URL
        value: ${DATABASE_URL}
      - key: SPRING_DATASOURCE_USERNAME
        value: ${DATABASE_USER}
      - key: SPRING_DATASOURCE_PASSWORD
        value: ${DATABASE_PASSWORD}
      - key: SPRING_PROFILES_ACTIVE
        value: prod
    autoDeploy: true

  - type: web
    name: aml-portal
    env: java
    rootDir: aml-portal
    buildCommand: ./mvnw clean package || mvn clean package
    startCommand: java -jar target/*.jar
    plan: starter
    envVars:
      - key: SPRING_DATASOURCE_URL
        value: ${DATABASE_URL}
      - key: SPRING_DATASOURCE_USERNAME
        value: ${DATABASE_USER}
      - key: SPRING_DATASOURCE_PASSWORD
        value: ${DATABASE_PASSWORD}
      - key: SPRING_PROFILES_ACTIVE
        value: prod
    autoDeploy: true

  - type: web
    name: aml-engine
    env: java
    rootDir: aml-engine
    buildCommand: ./mvnw clean package || mvn clean package
    startCommand: java -jar target/*.jar
    plan: starter
    envVars:
      - key: SPRING_DATASOURCE_URL
        value: ${DATABASE_URL}
      - key: SPRING_DATASOURCE_USERNAME
        value: ${DATABASE_USER}
      - key: SPRING_DATASOURCE_PASSWORD
        value: ${DATABASE_PASSWORD}
      - key: SPRING_PROFILES_ACTIVE
        value: prod
    autoDeploy: true

  - type: web
    name: aml-frontend
    env: static
    rootDir: .
    buildCommand: yarn install && yarn build
    staticPublishPath: dist
    envVars:
      - key: VITE_ADMIN_API_URL
        value: https://aml-admin.onrender.com
      - key: VITE_PORTAL_API_URL
        value: https://aml-portal.onrender.com
    autoDeploy: true

databases:
  - name: amlengine-db
    plan: starter
    postgresMajorVersion: 15 